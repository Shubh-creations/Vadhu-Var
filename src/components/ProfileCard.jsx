import React from 'react';
import ProfileDiscoveryCard from './ProfileDiscoveryCard';

export const ProfileCard = (props) => {
  return <ProfileDiscoveryCard {...props} />;
};

export default ProfileCard;
